# SMILE software factory

A distributable agent software factory: planning ladder, bead graph, deterministic
build loop with independent review, and a watchtower. Installs into any repo as a
Claude Code skill (`/smile`). Under construction; see `docs/SPEC.md` and
`docs/decisions.tsv`.
